- remplir les issues de github pour supprimer ce fichier todo
- feature : add note to specify if prod has problem (without necessary publication, like down, daily cron, etc...)
-- nouveau type de Row 'Notes'
-- Créer un classe abstraite \Object\RowRelated dont héritera \Object\RowRelated\Publication et \Object\RowRelated\Note puisqu'il y a beaucoup de choses communes
- feature : pouvoir noter une publication comme "prévue mais pas encore faite"
-- Dans ce cas pas de date (on mettra automatiquement now() à l'affichage)
- feature : permettre de configurer le mail dans la configuration générale + surcharger, si besoin, dans la config des projets
- feature : masquer la dernière colonne avec un bouton : voir les logs
- feature : set publications on prod tag instead preprod
-- Se baser sur la date de la dernière exécution du job Jenkins
- feature : can view differences between 2 publications (and revisions ?)
- check apc loaded when needed
- technique : utiliser un système de template
- technique : utiliser un système de traduction
- feature : notifications on publication row creation ?
- feature : prendre en charge git